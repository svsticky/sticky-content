"use client";

export default function Deploy() {
  return (
    <button className="btn btn--size-small btn--withoutPopup btn--style-pill" onClick={() => alert('Hello, world!')}>
      Deploy
    </button>
  )
}